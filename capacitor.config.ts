import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kapilash.touristexplorer',
  appName: 'Sri Lanka Tourist Explorer',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
